name: Build & Deploy

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      CI: false

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Check for changes in package files
        id: check_changes
        run: echo "::set-output name=changes::$(git diff --name-only <commit_hash_before> <commit_hash_after> | grep 'package-lock.json\|package.json' || true)"

      - name: Install dependencies if changes
        if: steps.check_changes.outputs.changes
        run: echo "Installing dependencies"

        